/** Ancho y padding compartidos en el portal cliente (sidebar + header). */
export const portalContentClass =
  'mx-auto w-full max-w-6xl px-4 sm:px-6';

/** Altura del panel de chat dentro del portal (header + sidebar + cabecera de página). */
export const portalChatPanelClass =
  'min-h-[520px] h-[min(720px,calc(100vh-11rem))]';
